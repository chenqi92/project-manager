{
  "name": "project-env-manager-sync",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Zero-knowledge self-hosted sync server for Project Env Manager. Stores only end-to-end-encrypted vault blobs; never sees plaintext or keys.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "tsx src/smoke.ts"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.5",
    "better-sqlite3": "^12.11.1",
    "hono": "^4.12.26"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.9.3",
    "tsx": "^4.22.4"
  }
}
