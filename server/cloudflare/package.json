{
  "name": "project-env-manager-sync-cf",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers + D1 build of the zero-knowledge sync server.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute envmanager_sync --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.12.26"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^4.0.0"
  }
}
