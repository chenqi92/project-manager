# Google Drive 同步用的 OAuth「Web 应用」客户端凭据。
# 构建时由 WXT 注入到 import.meta.env.WXT_*（见 lib/oauth.ts）；
# 复制本文件为 .env 并填入真实值。.env 已在 .gitignore，不会入库。
# 留空则 Google Drive 同步回退到用户在设置里自填的 client_id/secret。
WXT_GOOGLE_CLIENT_ID=
WXT_GOOGLE_CLIENT_SECRET=
